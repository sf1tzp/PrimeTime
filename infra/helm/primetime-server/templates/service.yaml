apiVersion: v1
kind: Service
metadata:
  name: {{ include "primetime-server.fullname" . }}
  labels:
    {{- include "primetime-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "primetime-server.selectorLabels" . | nindent 4 }}
