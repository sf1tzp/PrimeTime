apiVersion: v1
kind: Service
metadata:
  name: {{ include "moment-tally-server.fullname" . }}
  labels:
    {{- include "moment-tally-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moment-tally-server.selectorLabels" . | nindent 4 }}
